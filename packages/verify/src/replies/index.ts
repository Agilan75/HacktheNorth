/**
 * V06 — the extraction check (PRD §12). Not yet re-exported by the frozen
 * package barrel: in-package consumers import `./replies/index.js`; the barrel
 * line is requested in docs/contracts/requests/V06.md.
 */
export * from './fixtures.js';
export * from './score.js';
