/**
 * `npm run verify` (V08): the 10M layers A + B run. Prints throughput, the
 * count actually completed, and every violation or disagreement it kept.
 *
 * Stub frozen by W0-4; unit V08 replaces this body only.
 */
export function main(_argv: readonly string[]): Promise<number> {
  throw new Error('NOT_IMPLEMENTED:V08');
}
