/**
 * `npm run verify:llm` (V09): drives the layer-C runner and writes
 * VERIFICATION.md plus out/summary.json.
 *
 * Stub frozen by W0-4; unit V09 replaces this body only.
 */
export function main(_argv: readonly string[]): Promise<number> {
  throw new Error('NOT_IMPLEMENTED:V09');
}
