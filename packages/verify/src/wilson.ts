import type { WilsonInterval } from './types.js';

/**
 * Wilson score interval for the layer-C agreement rate (V07).
 * Stub frozen by W0-4; unit V07 replaces this body only.
 */
export function wilsonInterval(
  _successes: number,
  _trials: number,
  _confidence?: number,
): WilsonInterval {
  throw new Error('NOT_IMPLEMENTED:V07');
}
