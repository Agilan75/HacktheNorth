/** Stage 2 — normalize. Body owned by Run 1 unit E02. */
import type { CanonicalSubmission, FieldMap, LineOfBusiness, RawBundle } from '../types.js';

export function normalize(
  _bundle: RawBundle,
  _fieldMap: FieldMap,
  _lineOfBusiness: LineOfBusiness,
): CanonicalSubmission {
  throw new Error('NOT_IMPLEMENTED:E02');
}
