/** Stage 5 — contradict. Body owned by Run 1 unit E04. */
import type { CanonicalSubmission, Contradiction, Rulebook } from '../types.js';

export function contradict(
  _submission: CanonicalSubmission,
  _rulebook: Rulebook,
  _extensions?: Rulebook,
): Contradiction[] {
  throw new Error('NOT_IMPLEMENTED:E04');
}
