{
  "name": "@retrofit/federato",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Federato adapter (live + mock), query planner with trace, reference transcriptions, actions.",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "snapshot": "node --env-file-if-exists=../../.env --import tsx src/scripts/snapshot.ts"
  },
  "dependencies": {
    "@retrofit/contracts": "*",
    "@retrofit/engine": "*",
    "zod": "4.6.5"
  }
}
