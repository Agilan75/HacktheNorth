import type { ReactElement } from 'react';

import type { AttachedSweepPanelProps } from './types.js';

/**
 * PRD 10 (l) Attached photo or sweep, if any.
 *
 * Stub frozen by W0-4. Unit C11 replaces this body only — never the signature,
 * never the import list's shape, never this file's path.
 */
export function AttachedSweep(_props: AttachedSweepPanelProps): ReactElement {
  throw new Error('NOT_IMPLEMENTED:C11');
}
