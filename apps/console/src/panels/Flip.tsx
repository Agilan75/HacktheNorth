import type { ReactElement } from 'react';

import type { FlipPanelProps } from './types.js';

/**
 * PRD 10 (g) Minimal flip with new score and price.
 *
 * Stub frozen by W0-4. Unit C10 replaces this body only — never the signature,
 * never the import list's shape, never this file's path.
 */
export function Flip(_props: FlipPanelProps): ReactElement {
  throw new Error('NOT_IMPLEMENTED:C10');
}
