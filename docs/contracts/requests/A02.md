# Contract request — A02

**File:** `docs/contracts/OWNERSHIP.json` / `UNITS.json` (A02 `owns`).

**Change (additive):** assign `apps/api/src/llm/gemini.test.ts` to A02, then move the
`describe('offline: …')` blocks out of `apps/api/src/llm/gemini.live.test.ts` into it
(verbatim; imports unchanged), leaving only the `live: Gemini smoke` block in the live file.

**Why:** A02 owns no non-live test path. `*.live.test.ts` is excluded from the default run, so the
30 offline tests of the provider and `generateJson` (model fall-through, MAX_ROUNDS, MAX_TOKENS
retry with doubled budget, degrade rules) currently run only under `RUN_LIVE=1`.

**Workaround in place:** the offline suites sit in the live file and use an injected `fetchImpl` /
the fake provider, so they make no network calls. Run them with
`RUN_LIVE=1 VITEST_MAX_WORKERS=1 npx vitest run apps/api/src/llm/gemini.live.test.ts --project api -t offline`.
