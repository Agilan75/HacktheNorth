# Contract request — A06

**File:** `docs/contracts/OWNERSHIP.json` / `UNITS.json` (A06 `owns`).

**Change (additive):** assign `apps/api/src/llm/calls/draft-request.test.ts` and
`apps/api/src/llm/calls/extract-reply.test.ts` to A06, then move the `describe('offline: …')` blocks
out of `apps/api/src/llm/calls/extract-reply.live.test.ts` into them (verbatim), leaving only the
`live: A06 Gemini smoke` block in the live file.

**Why:** A06 owns no non-live test path. `*.live.test.ts` is excluded from the default run, so the 21
offline tests of both calls (template fallback, extra-ask rejection, G-4/G-5/G-6 parsing, quote check,
PDF cap, contradiction cap) run only under `RUN_LIVE=1`.

**Workaround in place:** the offline suites use the fake provider only (no network). Run them with
`RUN_LIVE=1 VITEST_MAX_WORKERS=1 npx vitest run apps/api/src/llm/calls/extract-reply.live.test.ts --project api -t offline`.
