# F02 contract request — packages/federato/tsconfig.json (frozen)

**File:** `packages/federato/tsconfig.json`

**Problem:** `npx tsc --noEmit -p packages/federato` reports TS6059/TS6307 because `fixtures/mini-snapshot.ts` sits outside `rootDir: "src"` and `include`, yet every `*.test.ts` that uses the fixture (F01's, F02's, F05's, F09's, …) imports it. These are the only errors in the package run; they are pre-existing and not F02-specific.

**Exact change (either):**
- exclude tests from the build program: add `"exclude": ["src/**/*.test.ts"]` (vitest type-checks nothing anyway), **or**
- set `"rootDir": "."` and add `"fixtures/**/*.ts"` to `include`.

**Workaround used:** none needed at runtime; vitest resolves the fixture fine. F02's own source files typecheck clean.
