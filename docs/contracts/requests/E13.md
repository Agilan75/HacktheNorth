# E13 — contract request

## File: `packages/engine/src/data.ts` (owned by W0-2 in OWNERSHIP.json, stubs tagged `NOT_IMPLEMENTED:E13`)

All nine loader bodies (`parseRulebook`, `parseVectorSpec`, `parseRatingTable`,
`parseQuestions`, `dataFilePath`, `readVectorSpec`, `readRulebook`,
`readRatingTable`, `readQuestions`) throw `NOT_IMPLEMENTED:E13`, but UNITS.json
gives E13 only `packages/engine/rules/commercial.json` and
`packages/engine/src/rules.commercial.test.ts`. E13 did not touch `data.ts`.

**Requested change:** either add `packages/engine/src/data.ts` to E13's `owns`
for a follow-up pass, or have the owner implement:

- `parse*`: `rulebookSchema.parse(json) as Rulebook` (likewise the vector-spec,
  rating-table and questions schemas in `schemas.ts`).
- `dataFilePath(kind, name)`: `fileURLToPath(new URL(\`../${kind}/${name}.json\`, import.meta.url))`
  (rules/, vectors/, rating/, questions/ sit beside `src/`).
- `read*`: `readFile(dataFilePath(...), 'utf8')` → `JSON.parse` → matching `parse*`.
  `readVectorSpec('commercial_property')` must map to `vectors/commercial.json`.

**Workaround used:** `rules.commercial.test.ts` reads the JSON with
`node:fs` and parses it through `rulebookSchema` directly.
