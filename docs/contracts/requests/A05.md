# Contract request — A05

**File:** `docs/contracts/OWNERSHIP.json` / `UNITS.json` (A05 `owns`).

**Change (additive):** assign `apps/api/src/llm/calls/schema-assist.test.ts` and
`apps/api/src/llm/calls/second-opinion.test.ts` to A05, then move the `describe('offline: schema-assist')`
and `describe('offline: second-opinion')` blocks out of
`apps/api/src/llm/calls/second-opinion.live.test.ts` into them verbatim (imports unchanged, drop the
`loadEnv`/`createGeminiProvider`/`MODELS` imports), leaving only `live: A05 Gemini smoke` in the live file.

**Why:** A05 owns no non-live test path. `*.live.test.ts` is excluded from the default run, so the
18 offline tests (sanitization, enum enforcement, degrade/no-degrade rules) run only under `RUN_LIVE=1`.

**Workaround in place:** offline suites sit in the live file and use the fake provider (no network).
Run: `RUN_LIVE=1 VITEST_MAX_WORKERS=1 npx vitest run apps/api/src/llm/calls/second-opinion.live.test.ts --project api -t offline`.
