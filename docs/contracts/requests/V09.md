# V09 contract request — reach the api LLM layer through a barrel

- **File:** `apps/api/src/index.ts` (the `@retrofit/api` entry, owned by A10) — or better, an
  `exports` entry `"./llm": "./src/llm/index.ts"` in `apps/api/package.json` (frozen).
- **Change (additive):** expose `secondOpinionCall`, `extractReplyCall`, `createGeminiProvider`,
  `createFakeLlm` and the `LlmProvider` / `GenerateJsonRequest` / `GenerateJsonResult` types, plus
  `getEnv` from `./env`, to `packages/verify`.
- **Why:** V09 (layer C) must call the A05 `second-opinion` call and build a Gemini provider from
  `env.ts`. `@retrofit/api`'s `exports` map exposes only `.`, which is the server entry and
  re-exports none of this.
- **Workaround in place:** `packages/verify/src/layer-c.ts`, `cli-llm.ts` and `layer-c.test.ts`
  import `../../../apps/api/src/llm/index` and `../../../apps/api/src/env` relatively, marked
  `TODO(contract)`. Typechecks (`tsc --noEmit -p packages/verify`, api is a project reference).
- **Also (informational):** `packages/verify/out/real-inputs.json` needs a producer — the 38 real
  property submissions rolled up to `NaiveInput` (see docs/decisions/V09.md §6). Suggest I1 or a
  CP2 step write it; V08 should write `packages/verify/out/run-summary.json`.
